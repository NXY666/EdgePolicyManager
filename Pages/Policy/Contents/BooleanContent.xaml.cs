using PolicyManager.Utils;

namespace PolicyManager.Pages.Policy.Contents;

public sealed partial class BooleanContent
{
    public BooleanContent(NotifyPolicyManager policyManager) : base(policyManager)
    {
        InitializeComponent();
    }
}